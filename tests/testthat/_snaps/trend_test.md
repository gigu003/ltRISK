# trend_test rejects an unidentified score

    Code
      trend_test(x, "group", score = rep(1, 3))
    Condition
      Error in `trend_test_one_range()`:
      ! score must contain at least two distinct values.

