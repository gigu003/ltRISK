% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_ltr_df.R
\name{calc_ltr_df}
\alias{calc_ltr_df}
\title{Estimate disease risk from a data frame}
\usage{
calc_ltr_df(
  data,
  ages = "ages",
  cancer = "cancer",
  cancer_death = "cancer_death",
  death = "death",
  pys = "pys",
  by = NULL,
  parallel = FALSE,
  workers = NULL,
  cluster = NULL,
  cache = c("retain", "clear", "none"),
  ...
)
}
\arguments{
\item{data}{A data frame containing the input variables.}

\item{ages, cancer, cancer_death, death, pys}{Column names in \code{data}. Each can be
supplied as a character string, a bare column name, or a character variable.}

\item{by}{Optional character vector of grouping columns. When supplied,
\code{calc_ltr_df()} calculates risks separately within each group and returns the
grouping columns together with the risk estimates. If \code{data} is a
\code{dplyr::grouped_df} and \code{by = NULL}, grouping variables are detected
automatically.}

\item{parallel}{Logical. If \code{TRUE}, calculate independent groups in parallel
using a cross-platform PSOCK cluster. Parallel execution is used only when
there is more than one group and more than one worker. Default is \code{FALSE}.}

\item{workers}{Number of parallel worker processes. \code{NULL} chooses up to one
fewer than the detected logical cores, capped by the number of groups.
\code{workers = 1} always uses the serial path. Ignored when \code{parallel = FALSE}.
Because each PSOCK worker is a separate R process, parallel execution is
most useful for many computationally intensive groups (for example, AMP or
DevCan gamma intervals) and uses additional memory.}

\item{cluster}{Optional PSOCK cluster created by
\code{\link[parallel:makePSOCKcluster]{parallel::makePSOCKcluster()}}. When supplied, it is reused and is not
stopped by \code{calc_ltr_df()}. This is useful for several consecutive grouped
calculations. \code{workers} is ignored when \code{cluster} is supplied.}

\item{cache}{Cache policy. \code{"retain"} (default) retains memoised results for
later calls. \code{"clear"} clears caches after the complete calculation.
\code{"none"} additionally clears caches after every group, limiting peak cache
growth in long grouped jobs. With an external \code{cluster}, clearing also
occurs on workers while leaving the cluster running.}

\item{...}{Additional arguments passed to \code{\link[=calc_ltr]{calc_ltr()}}, such as
\code{risk_func}, \code{type}, \code{maj_method}, \code{ci_method}, \code{age_start}, and \code{age_end}.}
}
\value{
A data frame returned by \code{\link[=calc_ltr]{calc_ltr()}}.
}
\description{
\code{calc_ltr_df()} is a data-frame interface to \code{\link[=calc_ltr]{calc_ltr()}}. It lets users
provide one data frame and specify the columns containing age-specific cancer
incidence, cancer mortality, all-cause mortality, and person-years.
}
\examples{
# One population using the package's standard column names
breast <- seer_example_data[seer_example_data$site == "Breast", ]
calc_ltr_df(
  breast,
  maj_method = "constant",
  ci_method = "none",
  age_start = c(0, 30, 50, 70),
  age_end = c(30, 50, 70, Inf),
  age_combine = "pairwise",
  digits = 4
)

# Calculate independent male and female risks in one call
all_by_sex <- seer_example_data[
  seer_example_data$site == "All" & seer_example_data$sex \%in\% c(1, 2),
]
calc_ltr_df(
  all_by_sex,
  by = "sex",
  maj_method = "constant",
  ci_method = "delta",
  age_start = 40,
  age_end = Inf,
  return_variance = TRUE
)

# Input columns can have other names
custom <- data.frame(
  age_group = breast$ages,
  cases = breast$cancer,
  cancer_deaths = breast$cancer_death,
  all_deaths = breast$death,
  population = breast$pys
)
calc_ltr_df(
  custom,
  ages = age_group,
  cancer = cases,
  cancer_death = "cancer_deaths",
  death = all_deaths,
  pys = population,
  ci_method = "none",
  maj_method = "constant"
)

\dontrun{
# Parallel grouped calculation and reusable cluster
calc_ltr_df(
  seer_example_data,
  by = c("site", "sex"),
  ci_method = "delta",
  parallel = TRUE,
  workers = 2
)

cl <- parallel::makePSOCKcluster(2)
calc_ltr_df(
  seer_example_data,
  by = c("site", "sex"),
  ci_method = "none",
  cluster = cl
)
parallel::stopCluster(cl)
}
}
