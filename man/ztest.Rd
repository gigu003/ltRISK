% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ztest.R
\name{ztest}
\alias{ztest}
\alias{ztest.data.frame}
\title{Test the difference in risk between two groups}
\usage{
ztest(
  x,
  y = NULL,
  group = NULL,
  ref = NULL,
  compare = NULL,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 6
)

\method{ztest}{data.frame}(
  x,
  y = NULL,
  group = NULL,
  ref = NULL,
  compare = NULL,
  alpha = 0.05,
  alternative = c("two.sided", "less", "greater"),
  digits = 6
)
}
\arguments{
\item{x}{A data frame returned by \code{\link[=calc_ltr]{calc_ltr()}} with
\code{return_variance = TRUE}, or a grouped result returned by \code{\link[=calc_ltr_df]{calc_ltr_df()}}.}

\item{y}{Optional. A second data frame returned by \code{\link[=calc_ltr]{calc_ltr()}} with
\code{return_variance = TRUE}. If \code{NULL}, \code{group} must be supplied and the two
groups are selected from \code{x}.}

\item{group}{Optional character string giving the grouping column in \code{x}.
Used when \code{y = NULL}.}

\item{ref}{Reference group value when \code{group} is supplied.}

\item{compare}{Comparison group value when \code{group} is supplied. The reported
difference is \code{risk_compare - risk_ref}.}

\item{alpha}{Alpha level for the confidence interval of the difference.}

\item{alternative}{Alternative hypothesis. Options are \code{"two.sided"},
\code{"less"}, or \code{"greater"}. For grouped input, the alternative is applied
to \code{risk_compare - risk_ref}; for two-input usage it is applied to
\code{risk_x - risk_y}.}

\item{digits}{Integer indicating the number of decimal places used to round
the returned estimates and test statistics.}
}
\value{
A data frame containing group labels, matched age ranges, risks,
risk difference, standard error of the difference, Z statistic, P value,
and confidence interval for the difference.
}
\description{
\code{ztest()} compares risk estimates from two independent groups using a
large-sample Z test. The inputs should be results returned by
\code{\link[=calc_ltr]{calc_ltr()}} or \code{\link[=calc_ltr_df]{calc_ltr_df()}} with \code{return_variance = TRUE}.
}
\examples{
data("seer_example_data")
breast <- seer_example_data[seer_example_data$site == "Breast", ]
female <- breast[breast$sex == 2, ]
male <- breast[breast$sex == 1, ]
if (nrow(female) > 0 && nrow(male) > 0) {
  risk_female <- calc_ltr(
    female$ages, female$cancer, female$cancer_death, female$death,
    female$pys, maj_method = "constant", return_variance = TRUE
  )
  risk_male <- calc_ltr(
    male$ages, male$cancer, male$cancer_death, male$death,
    male$pys, maj_method = "constant", return_variance = TRUE
  )
  ztest(risk_male, risk_female)
}
}
