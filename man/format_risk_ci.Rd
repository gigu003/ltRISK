% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_risk_ci.R
\name{format_risk_ci}
\alias{format_risk_ci}
\title{Format risk estimates with 95\% confidence intervals}
\usage{
format_risk_ci(
  risk,
  lower = NULL,
  upper = NULL,
  risk_col = "risk",
  lower_col = "lower",
  upper_col = "upper",
  name = "risk_95ci",
  digits = NULL,
  conf_level = 95,
  show_conf_level = FALSE,
  sep = ", ",
  na = NA_character_
)
}
\arguments{
\item{risk}{Numeric vector of risk estimates, or a data frame containing risk
estimates and confidence interval bounds.}

\item{lower}{Numeric vector of lower confidence interval bounds. When \code{risk}
is a data frame, this can be the lower-bound column name.}

\item{upper}{Numeric vector of upper confidence interval bounds. When \code{risk}
is a data frame, this can be the upper-bound column name.}

\item{risk_col}{Column name containing risk estimates when \code{risk} is a data
frame. Default is \code{"risk"}.}

\item{lower_col}{Column name containing lower confidence interval bounds when
\code{risk} is a data frame. Default is \code{"lower"}.}

\item{upper_col}{Column name containing upper confidence interval bounds when
\code{risk} is a data frame. Default is \code{"upper"}.}

\item{name}{Column name for the formatted output when \code{risk} is a data frame.
Default is \code{"risk_95ci"}.}

\item{digits}{Optional integer indicating the number of decimal places to
display. If \code{NULL} and \code{risk} is a data frame returned by \code{\link[=calc_ltr]{calc_ltr()}} or
\code{\link[=calc_ltr_df]{calc_ltr_df()}}, the calculation's \code{digits} setting is inherited. Otherwise,
values are converted with \code{\link[=as.character]{as.character()}} without additional rounding.}

\item{conf_level}{Confidence level displayed in the formatted text. Default
is \code{95}.}

\item{show_conf_level}{Logical. If \code{TRUE}, include the confidence level label
such as \verb{95\% CI} before the interval bounds. Default is \code{FALSE}.}

\item{sep}{Separator between lower and upper confidence interval bounds.
Default is \code{", "}.}

\item{na}{Character value used when any of \code{risk}, \code{lower}, or \code{upper} is
missing for an observation. Default is \code{NA_character_}.}
}
\value{
A character vector in the form \code{risk (lower, upper)}, or a
data frame with an added formatted column when \code{risk} is a data frame.
}
\description{
\code{format_risk_ci()} combines risk estimates and their lower and upper
confidence interval bounds into a character vector suitable for tables. When
\code{risk} is a data frame, the formatted value is added as a new column.
}
\examples{
format_risk_ci(risk = 12.3, lower = 10.1, upper = 14.5)
format_risk_ci(risk = 12.345, lower = 10.123, upper = 14.567, digits = 1)

breast <- seer_example_data[seer_example_data$site == "Breast", ]
res <- calc_ltr(
  ages = breast$ages, cancer = breast$cancer,
  cancer_death = breast$cancer_death, death = breast$death,
  pys = breast$pys, maj_method = "constant",
  ci_method = "delta", age_start = 0, age_end = Inf,
  digits = 2
)
res$risk_95ci <- format_risk_ci(res$risk, res$lower, res$upper, digits = 2)
format_risk_ci(res, digits = 2)
}
